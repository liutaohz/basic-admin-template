import React, { useRef, useState } from 'react';
import styles from './index.less';
const SearchProTable: React.FC<unknown> = () => {

  return (<>
    <div className={styles.pageConter}>
      SearchProTable
    </div>
  </>)
}
export default SearchProTable;